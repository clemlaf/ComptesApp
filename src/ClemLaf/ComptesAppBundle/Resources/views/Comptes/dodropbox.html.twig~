<select name="{{ na }}" {{ add }} 
	{% if add2 %}
	  onChange="javascript:ajax(rw{{ add2 }})"
	{% endif %}
	>  
  {% if z0 %}
    <option value="">{{ z0 }}</option>
  {% endif %}
  {% for list in res %}
    <option value={{list['id']}} {% if sele==list['id'] %}selected{% endif %}>{{ list['name'] }}</option> 
  {% endfor %}
</select>
